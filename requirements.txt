# DaBiT dependencies — verified on Python 3.11 + CUDA 12.4 (RTX 4090).
# A single `pip install -r requirements.txt` pulls the CUDA build of torch.
# For a different CUDA/CPU setup, change the index URL below accordingly
# (see https://pytorch.org/get-started/locally/).
--extra-index-url https://download.pytorch.org/whl/cu124

torch==2.6.0+cu124
torchvision==0.21.0+cu124

# pytorch_wavelets (focal-blur wavelet utilities) needs pywt and, because it
# still imports the legacy pkg_resources API, setuptools<81.
setuptools<81
pywavelets==1.9.0
pytorch-wavelets==1.3.0

# core scientific / vision stack
numpy==2.4.4
scipy==1.17.1
opencv-python==4.13.0.92
scikit-image==0.26.0
matplotlib==3.10.9
imageio==2.37.3
imageio-ffmpeg==0.6.0
av==17.0.1

# model / training utilities
addict==2.4.0
einops==0.8.2
future==1.0.0
pyyaml==6.0.3
requests==2.34.2
timm==1.0.27
yapf==0.43.0
tensorboard==2.20.0

# evaluation metrics
lpips==0.1.4
tqdm==4.68.1
